# Healthcare Claims Processing ETL - Python Dependencies
# This file contains all Python packages needed for our ETL pipeline
# Format: package==version (pinned versions ensure reproducible builds)

# === DATA PROCESSING LIBRARIES ===
pandas==2.1.4              # Data manipulation and analysis - perfect for ETL
numpy==1.26.2              # Numerical computing foundation
pyarrow==14.0.2            # Apache Arrow support for fast data processing

# === DATABASE CONNECTIVITY ===
pyodbc==5.0.1              # Microsoft SQL Server driver for Azure SQL
sqlalchemy==2.0.23         # SQL toolkit and ORM for database operations
azure-identity==1.15.0     # Azure authentication for secure database access

# === HEALTHCARE DATA FORMATS ===
hl7==0.4.5                 # HL7 healthcare message parsing
dicom2nifti==2.4.9         # Medical imaging format conversion (if needed)

# === CONFIGURATION AND LOGGING ===
pyyaml==6.0.1              # YAML configuration file parsing
python-dotenv==1.0.0       # Environment variable management
loguru==0.7.2              # Advanced logging with structured output

# === DATA VALIDATION ===
cerberus==1.3.5            # Data validation and schema enforcement
jsonschema==4.20.0         # JSON schema validation for API responses

# === TESTING FRAMEWORK ===
pytest==7.4.3             # Testing framework - runs our unit tests
pytest-cov==4.1.0         # Test coverage measurement
pytest-mock==3.12.0       # Mock objects for testing
pytest-xdist==3.5.0       # Parallel test execution

# === CODE QUALITY TOOLS ===
flake8==6.1.0              # Code linting and style checking
black==23.12.1             # Code formatting (optional but recommended)
isort==5.13.2              # Import statement sorting

# === SECURITY ===
safety==2.3.5              # Security vulnerability scanning
cryptography==41.0.8       # Cryptographic functions for data encryption

# === PERFORMANCE MONITORING ===
psutil==5.9.6              # System and process monitoring
memory-profiler==0.61.0    # Memory usage profiling

# === DEVELOPMENT TOOLS ===
jupyter==1.0.0             # Interactive development notebooks
ipython==8.19.0            # Enhanced Python REPL

# === API CLIENTS (for external integrations) ===
requests==2.31.0           # HTTP client for API calls
httpx==0.25.2              # Async HTTP client (if needed)

# === DATA SERIALIZATION ===
pickle5==0.0.12            # Enhanced pickle serialization
joblib==1.3.2             # Efficient object serialization for large datasets
